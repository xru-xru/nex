import ContentSelection from '../../../components/ContentSelection/ContentSelection';

function PortfolioContent() {
  return <ContentSelection preselect={true} />;
}

export default PortfolioContent;
